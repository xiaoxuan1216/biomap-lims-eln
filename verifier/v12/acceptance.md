# v12 · 分子克隆排板与产品整合

日期：2026-09-04。

## 交付范围

排板内嵌现有 `/workflows/:id` 编辑器：流程图与孔板视图原位切换，节点属性面板直接显示对应阶段孔板，项目原有流程卡片带方案摘要。独立导航入口已移除；`/cloning-planner` 仅兼容旧链接。支持动态数量、全流程孔板、条件推荐、悬停详情、跨板追踪、全板总览、JSON / CSV、业务流与节点关联、不可变历史快照和活动记录。

真实库存、设备执行、ELN 签署和结果放行未被模拟为已完成。规划数据显式为 `planningOnly: true`、`sourceMode: fictional`。

## 已验证

- `npm run verify`：lint、17 个测试文件 / 184 项测试、TypeScript、前端生产构建、后端打包均通过。保留原有 3Dmol eval 和较大 chunk 提示。
- 新增规则测试包括容量边界、成组额外板数、尾板均衡、34 个规模 / 条件组合、1536 目标 × 4 候选、目标完整性、孔位互斥和来源引用。
- 生成并审查 `0010_cloning_layout_plans.sql`：仅增加规划快照表和索引。版本化迁移执行成功；既有 Docker 应用已重启，ping 正常。
- `verifier/v12/api-check.mts` 在真实本地 API / MySQL 上验证：快照读回、项目 / 节点关联、历史不可变、幂等重试、改内容重试拒绝、旧版本拒绝、并发保存串行化、错误节点拒绝、未认证拒绝、viewer 写入拒绝。
- 保存前后样本数量、库存流水数和工作流节点 ID 完全一致；活动哈希链验证有效。
- 新增文案经 AST 检查：与原有 `en.ts` 无重复键，新增页面和规则字符串无缺少英文映射。
- 本地前端入口和正常登录页可以加载，未记录浏览器 JS 错误。

示例：项目 ID 7、流程 ID 22；紧凑快照 ID 1，推荐快照 ID 2。名称为“演示：分子克隆排板与 BioFlow 集成”，关联 PCR 节点 `n3`。

## 验证限制与后续状态

- 上一轮 QA 令牌登录操作曾被自动审批拒绝。本轮读取现有浏览器时，项目和流程页已经处于登录状态，因此在既有会话中继续验证，未执行登录、注入会话或更改认证。登录后交互与中英文走查现已完成，见下方补充验收。
- 生产依赖审计在 Docker 内等待 registry 响应时超过 60 秒；宿主机 pnpm 复核也未在限定窗口返回，已结束本任务启动的检查进程。尚无有效审计结论，不得记为“无漏洞”。

## 运行证据

- `verifier/runs/v12-cloning-verify.log`
- `verifier/runs/v12-cloning-api.log`
- `verifier/v12/api-checks.json`
- `verifier/runs/v12-cloning-audit.log`
- `verifier/runs/v12-cloning-audit-pnpm.log`

原有本地服务使用 Docker 挂载工作区与开发服务器。本次重启该既有容器，未切换端口、身份认证方式或部署拓扑。


## 流程内嵌补充验收

- 最新 `npm run verify` 全部通过：17 个测试文件、184 项测试、lint、TypeScript、前后端构建。未修改排板算法、API 或数据库结构。
- 现有 BioFlow 中输入 200 个样本，切到流程图再返回，数量和未保存草稿仍保留；恢复为 96 后继续节点验证。
- PCR 节点属性面板内直接显示 96 孔板。换到第二块 `PCR-PLAN-002` 后显示 48 个样本、2 个对照，从 `PCR-049` 起。
- 节点孔位键盘焦点触发样本详情浮层，核对 `PCR-002`、`PCR-PLAN-001:A02`、模板与正反向引物三个来源容器、共用试剂编码。鼠标悬停使用同一 Tooltip；本轮实际交互触发方式为键盘焦点。
- 在同一流程内保存“96 样本 · 流程内避边布局”，产生 V3，96 样本 / 21 板。读回显示创建时间 2026-09-04 17:30:42 和来源节点“基因片段 PCR 扩增”。
- 选择历史 V1 后，条件回到紧凑方案，PCR 首板显示 94 样本 + 2 对照；节点面板、画布摘要和全流程视图同步为 V1。V2 仍是 96 样本 / 19 板的推荐均衡方案。
- 中英文实测：节点摘要、图 / 孔板切换、排板条件、版本操作与流向区域正常翻译；用户输入的方案名和现有业务流 / 项目名保留原文。
- 200 样本 × 3 候选场景显示 52 块板；全部孔板弹窗列出各阶段并支持阶段筛选。紧凑方案 J / K 有 4 次目标跨板，推荐方案为 0。
- 项目 7 的“业务流追踪”原卡片显示 V3 / 96 样本 / 21 板；点击回到相同业务流内的孔板视图。项目既有“最近动态”自动显示本次保存事件。
- 国际化 AST 检查：2431 个字典键，无重复键，新增内嵌组件与兼容页面无漏译字符串。
- 本轮生产依赖审计设置 10 秒网络超时后仍返回 npm 审计端点超时；不能确认无高危漏洞。此项为未完成门禁。

补充证据：`verifier/runs/v12-embedded-verify.log`、`verifier/runs/v12-embedded-audit.log`、`verifier/runs/v12-embedded-i18n.json`。UI 证据由本任务 CUA 操作 / DOM 回读与截图提供。

服务重启后的复核：既有 Docker 应用返回 HTTP 200，重新加载 V3 成功；切回流程图时自动定位 PCR 节点，节点孔板显示 58 样本 + 2 对照及避让边缘孔。浏览器错误日志为空。
